# Task 5: Refactor ContextRouter Evidence

## QA Steps Performed

### Step 1: Verify no Discord imports remain
```bash
grep -c "import discord\|from discord" services/memory/context_router.py
```
Result: 0 ✓

### Step 2: Verify ContextRouter imports successfully
```bash
python3 -c "from services.memory.context_router import ContextRouter; print('OK')"
```
Result: OK ✓

## Changes Made

### File: services/memory/context_router.py

1. **Removed import:**
   - Removed `import discord`
   - Added `from core.types import AcoreChannel, AcoreUser`

2. **Updated detect_channel_type():**
   - Changed parameter type from `discord.abc.Messageable` to `AcoreChannel`
   - Replaced isinstance checks with `.type` comparisons:
     - `isinstance(channel, discord.DMChannel)` → `channel.type == "dm"`
     - `isinstance(channel, discord.Thread)` → `channel.type == "thread"`
     - `isinstance(channel, discord.VoiceChannel)` → `channel.type == "voice"`

3. **Updated get_context():**
   - Changed `channel` parameter from `discord.abc.Messageable` to `AcoreChannel`
   - Changed `user` parameter from `discord.User` to `AcoreUser`
   - Updated thread check: `isinstance(channel, discord.Thread)` → `channel.type == "thread"`

4. **Updated type hints:**
   - `participants: List[int]` → `participants: List[str]`
   - `Dict[int, datetime]` → `Dict[str, datetime]`
   - `Dict[int, str]` → `Dict[str, str]`
   - `_should_summarize()` channel_id: `int` → `str`
   - `_get_or_create_summary()` channel_id: `int` → `str`
   - `_get_parent_summary()` parameter: `discord.Thread` → `AcoreChannel`
   - `clear_voice_context()` channel_id: `int` → `str`

### File: core/types.py

Added `parent_id` field to `AcoreChannel` dataclass:
```python
parent_id: Optional[str] = None
```

This enables thread context inheritance (threads need to reference their parent channel).

## Verification

All isinstance checks replaced with `.type` string comparisons.
All Discord types replaced with Acore types.
No Discord imports remain in the file.
Import test passes successfully.
